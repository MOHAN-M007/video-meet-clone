# Video Meet Worker (Signaling Server)

This is a WebSocket-based signaling server for WebRTC meetings, designed to run on Cloudflare Workers.

## 🚀 Deploying

1. Install Wrangler
```bash
npm install -g wrangler
